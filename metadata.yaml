# Plugin name
name: cinder-unity
# Human-readable name for your plugin
title: Unity fuel plugin for Cinder
# Plugin version
version: '1.0.0'
# Description
description: Enables Unity as fuel Cinder backend
# Required fuel version
fuel_version: ['7.0', '8.0','9.0']
# Specify license of your plugin
licenses: ['Apache License Version 2.0']
# Specify author or company name
authors: ['DELL']
# A link to the plugin's page
homepage: 'https://github.com/openstack/fuel-plugin-unity'
# Specify a group which your plugin implements, possible options:
# network, storage, storage::cinder, storage::glance, hypervisor
groups: ['storage::cinder']
# Can be enabled after fuel installation
is_hotpluggable: true
# The plugin is compatible with releases in the list
releases:
  - os: ubuntu
    version: 2015.1.0-7.0
    mode: ['ha', 'multinode']
    deployment_scripts_path: deployment_scripts/
    repository_path: repositories/ubuntu
  - os: ubuntu
    version: liberty-8.0
    mode: ['ha', 'multinode']
    deployment_scripts_path: deployment_scripts/
    repository_path: repositories/ubuntu
  - os: ubuntu
    version: mitaka-9.0
    mode: ['ha', 'multinode']
    deployment_scripts_path: deployment_scripts/
    repository_path: repositories/ubuntu

# Version of plugin package
package_version: '3.0.0'
